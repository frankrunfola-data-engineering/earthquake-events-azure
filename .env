# Public endpoint; not a secret
API_BASE_URL="https://earthquake.usgs.gov/fdsnws/event/1/query"

# Local outputs (Bronze/Silver/Gold)
OUTPUT_DIR="data"

# Default run date label = today - LOOKBACK_DAYS
LOOKBACK_DAYS=2

LOG_LEVEL="INFO"
